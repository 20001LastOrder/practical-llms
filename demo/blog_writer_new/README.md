# Blog Writer Based on New Flow Policy
Installation: `pip install -r requirements.txt`

## Usage
1. Run `python flow.py`

> Current, the flow only works until `generate outline` after that, the rest of the flow is only for demo purpose.